version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building static site for Cognito OIDC tutorial"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: [] 
